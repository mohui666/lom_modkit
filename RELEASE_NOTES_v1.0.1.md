# lom_modkit v1.0.1

本版本修复自定义战役启动、战斗配置和编辑器预览中的实际错误，并收紧战役存档身份。Compiler、Editor 与 Runtime 版本统一为 `1.0.1`。

## 自 v0.7.0 以来的能力总览

- **编辑工作流**：可视化、多章节剧情编辑，覆盖 62 类节点；提供节点图、全局搜索、历史记录、崩溃恢复、项目模板、发布前检查和 AI 友好的 Story CLI。
- **内容制作**：支持自定义人物、多表情立绘、体型与朝向、背景/结局图片、音乐/音效/对白语音、内容包导入导出和未使用素材检查。
- **本地化**：Editor UI 与 Story 均支持简中、繁中、日文、韩文，包内提供明确的默认语言和回退链。
- **测试与调试**：支持从选中节点试玩、演出预览、路径模拟、条件/变量检查、结构化 Runtime 错误和脱敏诊断包；Compiler、Editor、Runtime 已纳入 push/PR CI。
- **安全 Runtime**：统一 ZIP 路径/重复项/大小/大小写校验，绑定 Story 与生成 Lua，隔离并清理 Lua 环境，保护热键迁移和 `runInBackground` 生命周期，并持续显示非官方来源披露。
- **战役与玩法**：接入经验证的原版 Combat/Battle 流程、战役触发器、隔离存档和持久变量，同时保持官方存档与“继续游戏”指针不被 MOD 覆盖。

## 重要兼容性说明

- `.lommod` 包格式升级为 v3，`manifest.campaign_id` 与 `campaign.new_game=true` 为必填。
- Runtime 会拒绝 v1/v2 包以及缺少、非法或前后不一致的 `campaign_id`，避免旧包静默共用或覆盖错误的存档槽。
- `campaign_id` 不会从包名或 `manifest.id` 自动推断。旧项目需要在 v1.0.1 Editor 中明确设置稳定 ID 并重新导出；同一战役后续版本应保持该 ID 不变。

## 修复

- 修复 Combat/Battle 位于章节首节点时可能永久停在“读取中”的竞态：原版 Story 会异步卸载 `Loading1`，Host 现在等待读取场景完全清空后再调用原版 `ChangeScene`。

- MOD 战役入口只记录用户选择，仍由明确的“开始新战役”操作启动，不再因浏览或选择列表项直接开局。
- MOD 手动存档和三类自动存档均按 `campaign_id` 命名空间隔离；最近使用的战役也按稳定身份恢复。
- 单挑的人物选项只决定战斗动画；血量、技能等其余战斗属性仍由作者自由填写。自定义人物缺少动画状态时回退 `idle`。
- 大规模战役的阵营总人数包含具名官方人物，避免具名人物被重复加算；Compiler/Editor 对人数、重复人物和非官方人物做一致校验。
- 修复编辑器表格下拉框被行高或列宽裁切、文字显示不完整的问题，并保留横向滚动能力。

## 发布与验证

- 新增跨 Compiler、Editor、Runtime 的回归测试，覆盖 v3 包拒绝、战斗配置、战役身份和生命周期行为。
- Windows 发布脚本会校验三端版本及冻结目录必需文件，拒绝缓存、样本包、用户配置和 symlink/junction 混入。
- 同名 `lom_modkit-v1.0.1_windows_x64.zip` 与 `.sha256` 默认不覆盖；维护者确认替换时必须显式使用 `-Force`。

发布文件：

- `lom_modkit-v1.0.1_windows_x64.zip`
- `lom_modkit-v1.0.1_windows_x64.zip.sha256`
